name: Deploy

on:
  workflow_dispatch:
  push:
    branches: [main]
    tags: ["v*"]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deployment notes
        run: |
          echo "Deploy web → Vercel"
          echo "Deploy api → Railway"
          echo "CDN / DNS → Cloudflare"
          echo "See docs/DEPLOYMENT.md"
